@model Book
<!--Create badge for prices-->
<div class="card card-outline-primary m-1 p-1">
    <div class="bg-faded p-1">
        <h4>
            @Model.Title
            <span class="badge badge-pill badge-primary" style="float:right">
                <small>@Model.Price.ToString("c")</small>
            </span>
        </h4>
    </div>
    <!--Create cards for each book-->
    <div class="card-text p-1">
        <b> @Model.Author</b><br />
        ISBN: @Model.Isbn.ToString()<br />
        Publisher: @Model.Publisher <br />
        Category: @Model.Category <br />
        Number of pages: @Model.PageCount <br />

        <!--Form to put book in cart-->
        <form method="post" asp-page="/Buy">
            <input type="hidden" asp-for="@Model.BookId"/>
            <input type="hidden" name="returnUrl" value="@ViewContext.HttpContext.Request.PathandQuery()"
            <span class="card-text">
                <button type="submit" class="btn btn-success btn-sm float-end">Add to Cart</button>
            </span>
        </form>
    </div>
</div>
